<main class="main-container">
  <section class="main-left"></section>
  <section class="main-side">
    <section class="main-side-container">
      <section class="main-side-top"></section>
      <nav class="nav">
        <%= link_to "Companies", companies_path %>
        <%= link_to "Jobs", jobs_path %>
        <%= link_to "Categories", categories_path %>
        <%= link_to "Dashboard", dashboard_path %>
        <%= link_to "Tags", tags_path %>
      </nav>
      <section class="main-side-bottom">
        <h1>All Jobs for <strong><%= @category.title %></strong></h1>
        <section class="companies">
          <% @category.jobs.each do |job| %>
            <div class="company-card">
              <%= link_to job.title, job_path(job) %><br>
              Company: <%= job.company.name %><br>
              Interest: <%= job.level_of_interest %><br>
              City: <%= job.city %><br>
            </div>
          <% end %>
      </section>
    </section>
  </section>
</main>
